- no ternary operator (`condition ? then : else`)
- ## Expression
- ```kotlin
  val max = if (a > b) {
      print("Choose a")
      a
  } else { // mandatory
      print("Choose b")
      b
  }
  ```
-